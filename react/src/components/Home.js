import React, { PureComponent} from 'react';

import HeaderPage from './Header';

class Home extends PureComponent {
    render() {
        return (
            <HeaderPage />
        );
    }
}

export default Home;
